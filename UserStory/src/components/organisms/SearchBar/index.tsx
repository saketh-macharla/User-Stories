import { Grid } from "@mui/material";
import React from "react";
const SearchBar = () => {
  return (
    <Grid>
      <Grid item>{/* Tab */}</Grid>
      <Grid item>{/* Dropdown */}</Grid>
    </Grid>
  );
};
export default SearchBar;
